#ifdef HAVE_CONFIG_H
#include <config.h>
#endif

#ifdef HAVE_STDARG_H
#include <stdarg.h>
#endif

#ifdef HAVE_ASSERT_H
#include <assert.h>
#endif

int intmax(int nargs, ... );
int intmin(int nargs, ... );
double doublemax(int nargs, ... );
double doublemin(int nargs, ... );
